(import (scheme base)
        ; (scheme char)
        ; (scheme lazy)
        ; (scheme inexact)
        ; (scheme time)
        ; (scheme read)
        (scheme write)
        ; (scheme case-lambda)
        ; (scheme cxr)
        )

;; This file is run by "yarn test:run" via run.ts.
;; Good for small tests.

(display "Hello world!")
